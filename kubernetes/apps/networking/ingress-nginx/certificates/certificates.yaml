---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: g-eye-io
  namespace: networking
spec:
  secretName: g-eye-io-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: g-eye.io
  dnsNames:
    - g-eye.io
    - "*.g-eye.io"
